> **Minimum viable outcome:**
> 1. The agentic loop works, or at least produces _some_ strategy (even if its not a good one).
> 2. The execution service picks up the strategy and starts running it in paper trading mode.
> 3. Market data telemetry is (WebSocket) live-streamed to the dashboard.
> 4. Execution service telemetry is live-streamed (WebSocket) to the dashboard.

---

## Status Key

- ✅ Implemented and working
- ⚠️ Partially implemented / has a known defect
- ❌ Not implemented

---

## Goal 1 — The agentic loop produces a strategy

The state machine, agent context building, LLM invocation, tool execution, artifact system, and
mailbox routing are all fully implemented. The loop runs Plan → Research → Analyze → Synthesize
and writes a validated `strategy.json` to disk each cycle. The loop now runs correctly end-to-end: Plan → Research → Analyze → Synthesize → WaitingForNextCycle → Evaluate (if history exists) → Plan.

### 1a — Core loop correctness

| #    | Item                                                                                                                                                                                                                                                                                                        | Status |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1a.1 | State machine, agent context builders, multi-turn LLM invocation, tool execution, artifact system, mailbox routing, per-agent/shared memory, loop resilience                                                                                                                                                | ✅      |
| 1a.2 | Tools: `web_search`, `web_fetch`, `bird`, `current_position`, `technical_indicators`                                                                                                                                                                                                                        | ✅      |
| 1a.3 | API for runtime control: pause, abort, force-cycle, inject context                                                                                                                                                                                                                                          | ✅      |
| 1a.4 | `LoopState.Execute` removed from enum and transition table. `Synthesize → WaitingForNextCycle` is the post-cycle transition; no `InvalidOperationException` is thrown. The loop no longer crashes after Synthesize.                                                                                         | ✅      |
| 1a.5 | `POST /api/override/resume` is implemented in `OverrideController` and calls `AgentRunnerService.Resume()`.                                                                                                                                                                                                 | ✅      |
| 1a.6 | All five agent prompt files (`plan_agent.md`, `research_agent.md`, `analysis_agent.md`, `synthesis_agent.md`, `evaluation_agent.md`) and all output templates (`plan.md`, `research.md`, `analysis.md`, `strategy.json`, `strategy.schema.json`, `evaluation.md`, `memory.md`) are present and well-formed. | ✅      |
| 1a.7 | `ExecuteParallelResearchAndAnalysisAsync` has been removed. Research and Analysis now both run sequentially through `ExecuteStepAsync`, consistent with every other cycle step. `StepRecord.StartTime` is set correctly from the step-start timestamp captured in `ExecuteStepAsync`.                       | ✅      |

### 1b — Evaluation as Step 0

The intended behaviour is: Evaluate runs **before** Plan each cycle, but only when there is a
previous cycle's `strategy.json` and execution telemetry to assess. On the very first cycle
(no historical data) Evaluation must be skipped and the loop should jump straight to Plan.

The state machine has been redesigned. Evaluate now runs **before** Plan, gated on `_latestPreviousCycleId != null`. The cycle order is `WaitingForNextCycle → Evaluate (if history) → Plan → Research → Analyze → Synthesize → WaitingForNextCycle`.

| #    | Item                                                                                                                                                                                                                                                                               | Status |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1b.1 | State machine now runs `WaitingForNextCycle → Evaluate (if history) → Plan → Research → Analyze → Synthesize → WaitingForNextCycle`. `Execute` state is removed. Both `WaitingForNextCycle → Evaluate` and `WaitingForNextCycle → Plan` transitions exist in the transition table. | ✅      |
| 1b.2 | `AgentRunnerService` tracks `_latestPreviousCycleId` (populated from `ArtifactManager.GetLatestCompletedCycleId()` at startup and after each cycle). All `WaitingForNextCycle` exits check `_latestPreviousCycleId != null` before routing to Evaluate vs. Plan.                   | ✅      |
| 1b.3 | `BuildEvaluationAgentContext(currentCycleId, previousCycleId)` reads `strategy.json` and `analysis.md` from the previous cycle's artifact directory. The `previousCycleId` is passed by `AgentRunnerService` from `_latestPreviousCycleId`.                                        | ✅      |
| 1b.4 | `StatusController` returns `cycleIntervalMinutes` from `_config.Cycle.ScheduleIntervalMinutes`. No hardcoded 6-hour value remains in the AgentRunner API. Note: `MonitoringDashboard/Controllers/AgentController.cs` still returns hardcoded stub values — addressed under Goal 4. | ✅      |

### 1c — Live-configurable cycle interval

The cycle interval should be adjustable from the dashboard without restarting any service.

| #    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Status |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1c.1 | `POST /api/config/cycle-interval` on AgentRunner validates and updates `_config.Cycle.ScheduleIntervalMinutes` in-process.                                                                                                                                                                                                                                                                                                                                                                            | ✅      |
| 1c.2 | `HandleNextCycleDelayAsync` uses a short-polling tick loop that re-reads `_config.Cycle.ScheduleIntervalMinutes` on every tick. A config update takes effect within one tick period.                                                                                                                                                                                                                                                                                                                  | ✅      |
| 1c.3 | **MonitoringDashboard has no endpoint or UI to set the cycle interval.** The AgentRunner `POST /api/config/cycle-interval` exists but there is no proxy route in `Crypton.Api.MonitoringDashboard` and no control widget in `Crypton.Web.Dashboard`. Fix: add `POST /api/agent/config/cycle-interval` to `AgentController`, proxy it to AgentRunner, and add an interval picker (e.g. in `LoopStatePanel`) in the web dashboard. This shares the `AgentRunnerClient` dependency with items 4c.2–4c.4. | ✅      |

### 1d — Ollama configuration

| #    | Item                                                                                                                                                                                                                                                                                                                      | Status |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1d.1 | Per-agent model name is configurable via `appsettings.json` (`Agents.{Name}.Model`).                                                                                                                                                                                                                                      | ✅      |
| 1d.2 | `AgentRunnerConfig.Ollama` section exists (`OllamaConfig` with `BaseUrl` and `TimeoutSeconds`). `AgentInvoker` reads `_config.Ollama.BaseUrl` to construct the endpoint. Configurable via `appsettings.json` or env var `Ollama__BaseUrl`.                                                                                | ✅      |
| 1d.3 | `docker-compose.yml` adds `extra_hosts: host-gateway:host-gateway` to the `agent-runner` service and sets `Ollama__BaseUrl=http://host-gateway:11434`. Ollama runs on the host workstation; containers reach it via the Docker bridge `host-gateway` alias without needing a sidecar container or GPU passthrough config. | ✅      |
| 1d.4 | **No per-agent Ollama override.** All agents share one base URL. Consider allowing a per-agent `BaseUrl` field in `AgentSettings` once the base config exists, to support routing lighter/heavier models to different endpoints.                                                                                          | ⚠️      |

---

## Goal 2 — Execution service picks up the strategy and runs paper trading

The Execution Service is structurally complete: paper trading adapter, position registry, entry/exit
evaluators, order router, strategy validator, and the full API surface are all implemented. There
are two critical gaps that prevent any order from ever executing in paper mode.

### 2a — Strategy handoff from AgentRunner to ExecutionService

| #    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Status |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| 2a.1 | `StrategyService` uses a `FileSystemWatcher` to hot-reload `strategy.json` from a configurable path. Validation and DSL pre-compilation on load are fully implemented.                                                                                                                                                                                                                                                                                                                           | ✅      |
| 2a.2 | **Shared volume path mismatch in `docker-compose.yml`.** `agent-runner` mounts to `/home/matthew-evans/.config/crypton/artifacts` and `execution-service` mounts to `/home/matthew-evans/.config/crypton/execution/artifacts`. They are different host paths, so `strategy.json` written by AgentRunner is never visible to ExecutionService. Fix: use a single named volume (e.g. `crypton_artifacts`) mounted at the same container path in both services, or align the host bind-mount paths. | ✅      |
| 2a.3 | **`ExecutionServiceConfig.BaseUrl` is registered in DI but never used.** The file-watch approach is the intended handoff mechanism (no HTTP push from AgentRunner to ExecutionService). This config key should be removed from `AgentRunnerConfig` to avoid confusion, or repurposed if a push notification on strategy write is ever added.                                                                                                                                                     | ✅      |

### 2b — Market data reaching the paper trading adapter

| #    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2b.1 | `PaperTradingAdapter` fill simulation (mid-price ± slippage, commission, synchronous ACK)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | ✅      |
| 2b.2 | `DelegatingExchangeAdapter` routes orders to paper adapter in paper mode                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ✅      |
| 2b.3 | `OperationModeService` persists paper/live state and exposes mode-change events                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ✅      |
| 2b.4 | **`IMarketDataSource` is wired to `NullMarketDataSource`**, which never emits snapshots. `PaperTradingAdapter` requires a cached `MarketSnapshot` to fill orders; without one it rejects every order with `"no_market_data"`. Fix: implement `MarketDataServiceClient : IMarketDataSource` that subscribes to the `Crypton.Api.MarketData` SignalR hub at `OnPriceUpdate` (hub path `/hubs/marketdata`) and forwards each tick as a `MarketSnapshot`. Register it in `ServiceCollectionExtensions.AddExecutionServiceCore()` replacing `NullMarketDataSource`. The hub URL must come from config (`MarketDataService__Url`) — see 2b.7. | ✅      |
| 2b.5 | **`DelegatingExchangeAdapter.SetLiveAdapters` is never called at startup.** `KrakenWebSocketAdapter` and `KrakenRestAdapter` are constructed manually inside `SetLiveAdapters` rather than being DI-registered and injected. Live mode is unreachable at runtime. (Not critical for paper mode, but the wiring must exist before live promotion is possible.)                                                                                                                                                                                                                                                                           | ✅      |
| 2b.6 | **`KrakenWebSocketAdapter` only subscribes to the public `ticker` channel** (`wss://ws.kraken.com/v2`). Live fill tracking requires subscribing to the `executions` channel on the **auth** endpoint `wss://ws-auth.kraken.com/v2` with a token obtained from `POST /0/private/GetWebSocketsToken` (token valid 15 min). Fill events with `exec_type=trade` or `exec_type=filled` must be forwarded to `OrderRouter` / `PositionRegistry` so live orders are reconciled without polling.                                                                                                                                                | ✅      |
| 2b.7 | **No `MarketDataService__Url` environment variable in the `execution-service` docker-compose block.** The `MarketDataServiceClient` (2b.4 fix) reads the hub URL from this config key; without it the SignalR connection cannot be established. Add `MarketDataService__Url=http://market-data-service:5002` to the `execution-service` env section and register the config key in `ExecutionServiceConfig`.                                                                                                                                                                                                                            | ✅      |

### 2c — Execution correctness gaps

| #    | Item                                                                                                                                                                                                                                                                                                                                                                                                 | Status |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2c.1 | Entry/exit evaluators, position sizing, portfolio risk enforcement                                                                                                                                                                                                                                                                                                                                   | ✅      |
| 2c.2 | `ReconciliationService` post-restart position sync                                                                                                                                                                                                                                                                                                                                                   | ✅      |
| 2c.3 | `SafeModeController` — market-sell all on activation                                                                                                                                                                                                                                                                                                                                                 | ✅      |
| 2c.4 | **`MetricsCollector` counters are never incremented.** `OrderRouter`, `EntryEvaluator`, and `ExitEvaluator` do not call any `_metrics.Record*()` method. `GET /metrics` and the SignalR `MetricsUpdate` stream always return zeroes.                                                                                                                                                                 | ✅      |
| 2c.5 | **`ExecutionEngine.CurrentMode` is hardcoded to `"paper"`.** `OperationModeService` is never queried, so the event log incorrectly labels all events as `"paper"` even after live promotion.                                                                                                                                                                                                         | ✅      |
| 2c.6 | **Short-sell positions are not opened in `PositionRegistry`.** `OrderRouter.ApplyFillAsync` only calls `OpenPosition(...)` for `Buy` fills. A filled short-direction order leaves the registry empty for that position.                                                                                                                                                                              | ✅      |
| 2c.7 | **`GetTradeHistoryAsync` timestamp bug.** Kraken returns `time` as a Unix **seconds** float. The current code calls `DateTimeOffset.FromUnixTimeMilliseconds((long)(tsUnix * 1000))`, producing timestamps ~46 years in the future. Fix: `DateTimeOffset.FromUnixTimeSeconds((long)tsUnix)`.                                                                                                         | ✅      |
| 2c.8 | **`OrderType` enum is missing `TakeProfit`, `TakeProfitLimit`, `TrailingStop`, `TrailingStopLimit`.** The `switch` in `PlaceOrderAsync` falls through to `"market"` for any of these types, silently submitting a market order instead of the intended conditional order. Fix: extend the enum and add string mappings (`take-profit`, `take-profit-limit`, `trailing-stop`, `trailing-stop-limit`). | ✅      |

### 2d — Live mode DI wiring

| #    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Status |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2d.1 | **`KrakenRestAdapter` and `KrakenWebSocketAdapter` are not registered in DI.** `ServiceCollectionExtensions.AddExecutionServiceCore()` never adds them; `DelegatingExchangeAdapter.SetLiveAdapters()` is never called. Fix: register both adapters as singletons in `AddExecutionServiceCore()` (reading `KrakenConfig` from options), then inject them into `DelegatingExchangeAdapter` via constructor or call `SetLiveAdapters` from an `IHostedService` startup hook. | ✅      |
| 2d.2 | **WS auth token lifecycle is unmanaged.** Before connecting to `wss://ws-auth.kraken.com/v2` (2b.6), a token must be fetched via `POST /0/private/GetWebSocketsToken` (REST, HMAC-signed). The token expires after 15 minutes of inactivity; implement a refresh mechanism — re-fetch on WS reconnect and proactively before expiry — to avoid authentication gaps during long-running sessions.                                                                          | ✅      |

### 2e — CLI mode and developer tooling

The Execution Service must be runnable as a one-shot CLI tool (`dotnet run -- --cli <command>`) using the same configuration loading pipeline as AgentRunner. This is required for live testing: once the service is built, individual order flows and reconciliation runs must be testable from the terminal without starting the full HTTP server stack.

| #    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Status |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2e.1 | **Configuration loading must follow the same priority chain as AgentRunner.** Resolution order: (1) `--env-file <path>` CLI arg (supports `~/` expansion); (2) `~/.config/crypton/.env`; (3) walk-up `.env` from cwd; (4) environment variables using `__` hierarchy convention; (5) `appsettings.json`. Short-form env var aliases must be mapped at startup before the host builder reads `IConfiguration`: `KRAKEN_API_KEY` → `ExecutionService__Kraken__ApiKey`, `KRAKEN_API_SECRET` → `ExecutionService__Kraken__ApiSecret`, `MARKET_DATA_URL` → `MarketDataService__Url`. | ✅      |
| 2e.2 | **`--cli` flag must switch to CLI mode.** When present, `Program.cs` instantiates services directly (same pattern as AgentRunner), runs the requested CLI command, and exits — the ASP.NET host and HTTP server are never started. A `CliRunner` class analogous to `AgentRunner.Cli.CliRunner` owns all command dispatch.                                                                                                                                                                                                                                                      | ✅      |
| 2e.3 | **CLI commands must cover the key live-test scenarios:** `status` (print operation mode, active strategy summary, open positions, portfolio P&L); `run-order --symbol <pair> --side buy\|sell --type market\|limit [--quantity <qty>] [--price <px>] [--verbose]` (place a single order through the full order routing path and print the fill result or rejection reason); `set-mode paper\|live` (change and persist operation mode); `reconcile` (trigger `ReconciliationService` once, print the outcome including any position corrections).                               | ✅      |
| 2e.4 | **Verbose console logging in CLI mode.** When `--verbose` is passed, Serilog writes at `Debug` level to stdout. Full request/response payloads (REST calls or WS messages), fill events, evaluator decisions, and reconciliation results must be visible. In non-verbose CLI mode, `Information`+ only. In server (non-CLI) mode, logging behaviour is unchanged from the current implementation.                                                                                                                                                                               | ✅      |

---

## Goal 3 — Market data telemetry live-streamed to the dashboard

This goal is the most complete. The full pipeline from Kraken WebSocket → MarketData SignalR hub
→ MonitoringDashboard bridge → DashboardHub → Web Dashboard is wired and working for price ticks.

| #   | Item                                                                                                                                                                                                                                                                        | Status |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 3.1 | `KrakenExchangeAdapter` subscribes to `ticker`, `book`, and `trade` channels via `wss://ws.kraken.com` with auto-reconnect and circuit breaker                                                                                                                              | ✅      |
| 3.2 | `MarketDataHub` (`/hubs/marketdata`) broadcasts `OnPriceUpdate`, `OnOrderBookUpdate`, `OnTrade`, `OnBalanceUpdate`, `OnConnectionStatus`                                                                                                                                    | ✅      |
| 3.3 | `MonitoringDashboard` connects to MarketData SignalR on startup and bridges `OnPriceUpdate` → `DashboardHub.PriceUpdated`                                                                                                                                                   | ✅      |
| 3.4 | Web Dashboard `signalr.ts` subscribes to `DashboardHub` and delivers price ticks to `PriceTickerPanel` / `PriceChartPanel`                                                                                                                                                  | ✅      |
| 3.5 | REST proxy endpoints for prices, indicators, OHLCV, portfolio summary, holdings are live (no stubs)                                                                                                                                                                         | ✅      |
| 3.6 | **`OnOrderBookUpdate` and `OnTrade` events are not bridged** from MarketData hub to `DashboardHub`. The MonitoringDashboard's `Program.cs` only subscribes to `OnPriceUpdate`. If the dashboard needs order-book or trade-tape panels, these events must also be forwarded. | ✅      |
| 3.7 | **Macro signals endpoint (`GET /api/market/macro`) is fully hardcoded** (Fear & Greed = 65, BTC dominance = 52.3%). No live data source is connected.                                                                                                                       | ✅      |

---

## Goal 4 — Execution service telemetry live-streamed to the dashboard

The Execution Service SignalR infrastructure (`ExecutionHub` + `ExecutionHubBroadcaster`) is fully
implemented and pushes five real-time streams. The MonitoringDashboard has no connection to it
at all; every agent, strategy, performance, and execution panel in the Web Dashboard is fed by
hardcoded stubs.

### Goal 4 — Implementation Sequence

All remaining ❌ items fall into three phases, ordered by dependency:

**Phase A — ExecutionServiceClient (resolves 4b.2, 4b.3, 4b.4, 4b.5)**
1. Add `ExecutionService:Url` to `MonitoringDashboard/appsettings.json`.
2. Create `Services/ExecutionServiceClient.cs`: SignalR client connecting to `ExecutionHub` at `/hubs/execution`, subscribing to the `status` and `positions` groups; raises `OnStatusUpdate` and `OnPositionUpdate` events; exposes HTTP proxy methods `GetPositionsAsync()`, `GetTradesAsync()`, `GetStrategyAsync()` against the ExecutionService REST API.
3. Register in `Program.cs` as singleton; connect in background retry loop; wire `OnPositionUpdate` → `DashboardHub.Clients.All.PositionUpdated()`.
4. Replace `PortfolioController.GetPositions()` and `GetTrades()` stubs to proxy through `ExecutionServiceClient`.
5. Replace `StrategyController.GetCurrent()` stub to proxy through `ExecutionServiceClient`.

**Phase B — AgentRunnerClient (resolves 4c.2, 4c.4, 4c.5)**
6. Create `Services/AgentRunnerClient.cs`: HTTP client targeting AgentRunner REST; exposes `GetStatusAsync()` (`GET /api/status`), `GetCyclesAsync()` (`GET /api/cycles`), `GetCycleDetailsAsync()` (`GET /api/cycles/{id}`).
7. Register in `Program.cs`; background polling loop (every 5 s) pushes `DashboardHub.AgentStateChanged` when state transitions are detected.
8. Replace `AgentController.GetState()` and `GetLoopStatus()` stubs: transform live `GET /api/status` + `GET /api/cycles` data into `AgentState`/`LoopStatus` models.
9. Replace `PerformanceController` stubs: `GetCurrentCycle()` and `GetLifetime()` aggregate closed trades from `ExecutionServiceClient.GetTradesAsync()`; `GetCycles()` enumerates AgentRunner cycle list; `GetEvaluation()` reads `evaluation.md` artifact from the latest completed cycle via `GetCycleDetailsAsync()`.

**Phase C — docker-compose wiring (resolves 4a.4, 4c.3)**
10. Add `ExecutionService__Url=http://execution-service:5004` and `AgentRunner__Url=http://agent-runner:5003` to the `monitoring-dashboard-api` environment block.
11. Add `depends_on: agent-runner (service_started), execution-service (service_healthy)` to `monitoring-dashboard-api`.

### 4a — Connect MonitoringDashboard to ExecutionService

| #    | Item                                                                                                                                                                                                                                                                                                                                                                                                          | Status |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 4a.1 | `ExecutionHub` (`/hubs/execution`) with `StatusUpdate` (2 s), `MetricsUpdate`, `EventLog`, and `PositionUpdate` streams fully implemented                                                                                                                                                                                                                                                                     | ✅      |
| 4a.2 | `ExecutionHubBroadcaster` hosted service wires position, event, and timer callbacks to hub pushes                                                                                                                                                                                                                                                                                                             | ✅      |
| 4a.3 | **MonitoringDashboard has no `ExecutionServiceClient`** and no connection to the ExecutionService. Add an `ExecutionServiceClient` (analogous to `MarketDataServiceClient`) that: connects to `ExecutionHub`, subscribes to all four streams, and raises events for the dashboard layer to consume. The ExecutionService URL must also be injected via config and `docker-compose.yml` environment variables. | ✅      |
| 4a.4 | `docker-compose.yml` now sets `ExecutionService__Url=http://execution-service:5004` and `AgentRunner__Url=http://agent-runner:5003` on `monitoring-dashboard-api`, and adds `depends_on: execution-service (service_healthy), agent-runner (service_started)`.                                                                                                                                                                                                                                             | ✅      |

### 4b — Wire live data to DashboardHub and backend controllers

| #    | Item                                                                                                                                                                                                                                                                      | Status             |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| 4b.1 | `DashboardHub` interface declares `PositionUpdated`, `AgentStateChanged`, `ToolCallStarted`, `ToolCallCompleted`, `ReasoningUpdated`, `StrategyUpdated`, `CycleCompleted`, `EvaluationCompleted`                                                                          | ✅ (interface only) |
| 4b.2 | `ExecutionServiceClient.OnPositionUpdate` callback in `Program.cs` maps the incoming `JsonElement` to `MonitoringDashboard.Models.Position` and pushes `DashboardHub.Clients.All.PositionUpdated()`. The AgentRunner polling loop pushes `AgentStateChanged` on every state transition. | ✅                  |
| 4b.3 | `GET /api/portfolio/positions` proxies `ExecutionService GET /positions` via `ExecutionServiceClient.GetPositionsAsync()`. Returns raw snake_case JSON from `OpenPosition`.                                                                                                                                                  | ✅      |
| 4b.4 | `GET /api/portfolio/trades` proxies `ExecutionService GET /trades` via `ExecutionServiceClient.GetTradesAsync()`. Returns raw snake_case JSON from `ClosedTrade`.                                                                                                                                                        | ✅      |
| 4b.5 | `GET /api/strategy/current` proxies `ExecutionService GET /strategy` (returns 404 if no strategy loaded). `GET /api/strategy/history` returns an empty list (ExecutionService has no history endpoint).                                                                                                                             | ✅      |

### 4c — Connect MonitoringDashboard to AgentRunner

| #    | Item                                                                                                                                                                                                                                                                                                                                                       | Status |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 4c.1 | `AgentRunner` exposes full status, cycle history, mailboxes, errors, and metrics via REST                                                                                                                                                                                                                                                                  | ✅      |
| 4c.2 | `Services/AgentRunnerClient.cs` added: HTTP client exposing `GetStatusAsync()` (`GET /api/status`), `GetCyclesAsync()` (`GET /api/cycles`), `GetCycleDetailsAsync()` (`GET /api/cycles/{id}`). Registered as `IAgentRunnerClient` singleton in `Program.cs`. Background polling loop (5 s) pushes `DashboardHub.AgentStateChanged` on every loop-state transition.                                                                                                                                                                                                                                                       | ✅      |
| 4c.3 | `docker-compose.yml` sets `AgentRunner__Url=http://agent-runner:5003` on `monitoring-dashboard-api` (see 4a.4). `depends_on` added (see 4a.4).                                                                                                                                                                                                                                                    | ✅      |                                                        | ❌      |
| 4c.4 | `GET /api/agent/state` and `/api/agent/loop` now transform live data from `AgentRunnerClient.GetStatusAsync()` + `GetCyclesAsync()`. `/api/agent/toolcalls` and `/api/agent/reasoning` return empty lists (AgentRunner does not persist structured tool-call or token-level reasoning traces to disk).                                                                                                                                  | ✅      |
| 4c.5 | `PerformanceController` replaced: `GetCurrentCycle()` and `GetLifetime()` aggregate closed trades from `ExecutionServiceClient.GetTradesAsync()`; `GetCycles()` enumerates the AgentRunner cycle list; `GetEvaluation()` reads `evaluation.md` from the latest cycle artifact via `AgentRunnerClient.GetCycleDetailsAsync()`.                                                                                                            | ✅      |

---

## Infrastructure / Cross-Cutting

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Status |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| I.1 | `docker-compose.yml` defines all five services with health checks and restart policies                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ✅      |
| I.2 | Ollama runs directly on the host workstation (not in a container). The `agent-runner` service in `docker-compose.yml` reaches it via the Docker bridge `host-gateway` alias (`extra_hosts: host-gateway:host-gateway`, `Ollama__BaseUrl=http://host-gateway:11434`). No sidecar or GPU passthrough is required. This is the intended and fully implemented deployment model — see 1d.3.                                                                                                                                                                                                                                                                                                                                                                                                                                | ✅      |
| I.3 | **`strategy.json` shared volume is broken** (see 2a.2). The AgentRunner and ExecutionService mount different host paths, so the file-watch handoff never fires.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | ✅      |
| I.4 | **`monitoring-dashboard-web` has no `VITE_API_BASE_URL` set** in `docker-compose.yml`. The React app's `api.ts` base URL is baked in at build time; if not set it defaults to `localhost` and will fail in Docker. Fix at build time via `--build-arg` or at runtime via nginx rewrite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ✅      |
| I.5 | **`execution-service` has no `depends_on: agent-runner`** in `docker-compose.yml`, but it relies on the shared artifact volume being populated before a strategy can be loaded. This is not a hard runtime blocker but means the first startup ordering is undefined.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ✅      |
| I.6 | **Full unit test coverage is required for all service code.** `Crypton.Api.ExecutionService.Tests` must cover: `KrakenRestAdapter` (HMAC signing, all order types, balance mapping, error handling, rate-limit backoff — all via mocked `HttpMessageHandler`); `KrakenWebSocketAdapter` (subscribe, reconnect, ticker parsing — via `ProcessMessageAsync`); `PaperTradingAdapter` (fill simulation, slippage, no-market-data rejection); `DelegatingExchangeAdapter` (paper/live routing); `OrderRouter` + `EntryEvaluator` + `ExitEvaluator`; `PositionRegistry`; `StrategyService`; `ReconciliationService`; `SafeModeService`; and all `CliRunner` commands. No `[ExcludeFromCodeCoverage]` on business logic. The same standard applies retroactively to `Crypton.Api.AgentRunner.Tests` for any newly added code. | ✅      |