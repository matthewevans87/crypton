services:
  agent-runner:
    build:
      context: .
      dockerfile: src/agent_runner/Dockerfile
    container_name: crypton-agent-runner
    ports:
      - "5003:5003"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://0.0.0.0:5003
      - BRAVE_SEARCH_API_KEY=${BRAVE_SEARCH_API_KEY}
      - AGENT_RUNNER_API_KEY=${AGENT_RUNNER_API_KEY}
      - MarketDataService__Url=http://host.docker.internal:5002
    volumes:
      - agent-runner-data:/app/artifacts
      - agent-runner-logs:/app/logs
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "5"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5003/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  market-data-service:
    build:
      context: .
      dockerfile: src/market_data_service/MarketDataService/Dockerfile
    container_name: crypton-market-data-service
    ports:
      - "5002:5002"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://0.0.0.0:5002
      - KRAKEN_API_KEY=${KRAKEN_API_KEY}
      - KRAKEN_SECRET_KEY=${KRAKEN_SECRET_KEY}
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5002/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  monitoring-dashboard-api:
    build:
      context: .
      dockerfile: src/monitoring_dashboard/backend/MonitoringDashboard.Api/Dockerfile
    container_name: crypton-monitoring-dashboard-api
    ports:
      - "5001:5001"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://0.0.0.0:5001
      - MarketDataService__Url=http://market-data-service:5002
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  monitoring-dashboard-web:
    build:
      context: .
      dockerfile: src/monitoring_dashboard/frontend/Dockerfile
    container_name: crypton-monitoring-dashboard-web
    ports:
      - "5000:5000"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  agent-runner-data:
  agent-runner-logs:
